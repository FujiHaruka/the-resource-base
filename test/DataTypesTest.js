/**
 * Test for DataTypes.
 * Runs with mocha.
 */
'use strict'

const DataTypes = require('../lib/DataTypes')
const { ok, equal } = require('assert')

describe('data-types', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
